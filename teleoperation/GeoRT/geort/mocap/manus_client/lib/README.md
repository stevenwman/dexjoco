Optional prebuilt Manus shared libraries are distributed as release assets instead of staying in Git.

From the repository root, restore them with:

```bash
python scripts/download_assets.py --bundle geort-manus-libs
```
